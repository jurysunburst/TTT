'use strict';

describe('mainCtrl', function() {

   /* var mockBoardMoveResource, $httpBackend;
    beforeEach(module('ttt'));

    beforeEach(function () {
        inject(function ($injector) {
            $httpBackend = $injector.get('$httpBackend');
            mockBoardMoveResource = $injector.get('BoardMove');
        })
    });

    describe('getBoard', function () {
        it('should get board', inject(function () {
            console.log(1, mockBoardMoveResource)
            $httpBackend.expectGET('/api/boardMove')
                .respond([{
                    Status: 0,
                    Board: [0, 0]
                }]);
            console.log(2)
            var result ;
                mockBoardMoveResource.get().$promise.then(function (board) {
                    result = board;
                    expect(result).toEqual(0);
            });;
            console.log(3)
            //$httpBackend.flush();


            expect(result).toEqual(0);
        }));

    });
*/
/*

    var $scope;
    var controller;

    beforeEach(function() {

        module("ttt");

        inject(function(_$rootScope_, $controller) {

            $scope = _$rootScope_.$new();
            controller = $controller("mainCtrl", {$scope: $scope});

        });

    });

        it('status changed', function() {
            console.log($scope.message);
            $scope.board = {Status:1};
            $scope.$apply();
            console.log($scope.message);
            expect($scope.message.header).toEqual('Congrats!');
        });*/
});